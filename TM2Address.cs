namespace TM2Driver
{
    public enum ModuleAddress : byte
    {
        Addr1 = 0x1,
        Addr2 = 0x2,
        Addr3 = 0x3,
        Addr4 = 0x4,
    }

    public enum ModuleChannel
    {
        CH1,
        CH2
    }
}
